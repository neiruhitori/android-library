# 🔌 Diagram Koneksi Android ↔ Laravel API

## 📊 Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    ANDROID APPLICATION                       │
│                  (d:\AndoidStudio\Perpustakaan)              │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ HTTP Requests
                            │ (Retrofit + OkHttp)
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    LARAVEL API SERVER                        │
│              (c:\laragon\www\sistem-library)                 │
│                  http://localhost:8000                       │
└─────────────────────────────────────────────────────────────┘
                            │
                            │ Eloquent ORM
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    MySQL DATABASE                            │
│                   (sistem_library)                           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Diagram

### 1. Get All Books Flow

```
[Android App]                [Laravel API]              [Database]
     │                            │                          │
     │ GET /api/buku             │                          │
     ├──────────────────────────>│                          │
     │                            │ SELECT * FROM bukus      │
     │                            ├─────────────────────────>│
     │                            │                          │
     │                            │<─────────────────────────┤
     │                            │ [Data Buku]              │
     │                            │                          │
     │                            │ Format foto_url          │
     │                            │ Prepare JSON Response    │
     │                            │                          │
     │<──────────────────────────┤                          │
     │ JSON Response              │                          │
     │ {success, message, data}   │                          │
     │                            │                          │
     ↓                            ↓                          ↓
[Display in                  [Controller]              [MySQL]
 RecyclerView]               [BukuApiController]
```

### 2. Get Book Detail Flow

```
[Android App]                [Laravel API]              [Database]
     │                            │                          │
     │ GET /api/buku/1           │                          │
     ├──────────────────────────>│                          │
     │                            │ SELECT * FROM bukus      │
     │                            │ WHERE id = 1             │
     │                            ├─────────────────────────>│
     │                            │                          │
     │                            │<─────────────────────────┤
     │                            │ [Single Buku Data]       │
     │                            │                          │
     │<──────────────────────────┤                          │
     │ JSON Response              │                          │
     │                            │                          │
     ↓                            ↓                          ↓
[Show Detail]               [Controller]              [MySQL]
```

### 3. Search Books Flow

```
[User Input]                [Android App]              [Laravel API]
     │                            │                          │
     │ "Laskar"                   │                          │
     ├───────────────────────────>│                          │
     │                            │ GET /api/buku/search?    │
     │                            │     q=Laskar             │
     │                            ├─────────────────────────>│
     │                            │                          │
     │                            │         WHERE judul      │
     │                            │         LIKE '%Laskar%'  │
     │                            │                          │
     │                            │<─────────────────────────┤
     │                            │ [Filtered Results]       │
     │<───────────────────────────┤                          │
     │ Update RecyclerView        │                          │
     ↓                            ↓                          ↓
```

---

## 📱 Android App Structure

```
┌──────────────────────────────────────────────────┐
│              SplashActivity                       │
│  • Dark purple background                         │
│  • App logo                                       │
│  • Welcome text                                   │
│  • Tap to continue                                │
└──────────────────────────────────────────────────┘
                    │ (tap anywhere)
                    ↓
┌──────────────────────────────────────────────────┐
│            DashboardActivity                      │
│  ┌────────────────────────────────────────────┐  │
│  │  Search Box (filter real-time)             │  │
│  └────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────┐  │
│  │  RecyclerView (BukuAdapter)                │  │
│  │  ┌──────────────────────────────────────┐  │  │
│  │  │ [Cover] Judul Buku                   │  │  │
│  │  │         by Penulis                   │  │  │
│  │  │         2024 [Tahunan]               │  │  │
│  │  │         Description...               │  │  │
│  │  └──────────────────────────────────────┘  │  │
│  │  ┌──────────────────────────────────────┐  │  │
│  │  │ [Cover] Another Book                 │  │  │
│  │  └──────────────────────────────────────┘  │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
                    │ (click buku)
                    ↓
┌──────────────────────────────────────────────────┐
│          DetailBukuActivity                       │
│  ┌────────────────────────────────────────────┐  │
│  │       [Large Book Cover Image]             │  │
│  └────────────────────────────────────────────┘  │
│  Judul Buku                                      │
│  📝 Penulis: Andrea Hirata                       │
│  📅 Tahun: 2024                                  │
│  🏷️ Tipe: [Tahunan]                              │
│  ┌────────────────────────────────────────────┐  │
│  │ Description:                               │  │
│  │ Lorem ipsum dolor sit amet...              │  │
│  └────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────┘
```

---

## 🔧 Laravel API Structure

```
sistem-library/
│
├── app/
│   ├── Http/
│   │   └── Controllers/
│   │       └── Api/
│   │           └── BukuApiController.php  ← API Endpoints
│   │               ├── index()      → GET /api/buku
│   │               ├── show($id)    → GET /api/buku/{id}
│   │               ├── search()     → GET /api/buku/search
│   │               ├── store()      → POST /api/buku
│   │               ├── update($id)  → PUT /api/buku/{id}
│   │               └── destroy($id) → DELETE /api/buku/{id}
│   │
│   └── Models/
│       └── Buku.php                   ← Model
│           ├── $table = 'bukus'
│           ├── $fillable = [...]
│           └── relationships()
│
├── routes/
│   └── api.php                        ← API Routes
│       └── Route::prefix('buku')
│
└── database/
    └── migrations/
        └── create_bukus_table.php     ← Database Schema
```

---

## 🌐 Network Configuration

### Untuk Emulator (10.0.2.2):
```
┌─────────────────┐         ┌─────────────────┐
│  Emulator       │         │  Host Machine   │
│  Android App    │         │  Laravel Server │
│                 │         │                 │
│  10.0.2.2:8000 ────────> │  localhost:8000 │
│                 │         │  127.0.0.1:8000 │
└─────────────────┘         └─────────────────┘

RetrofitClient BASE_URL: "http://10.0.2.2:8000/"
```

### Untuk Device Fisik (Same WiFi):
```
┌─────────────────┐         ┌─────────────────┐
│  Physical       │   WiFi  │  Host Machine   │
│  Android Device │ ◄─────► │  Laravel Server │
│                 │         │                 │
│  192.168.1.X   ────────> │  192.168.1.5    │
│                 │         │  :8000          │
└─────────────────┘         └─────────────────┘

RetrofitClient BASE_URL: "http://192.168.1.5:8000/"
```

---

## 📦 Data Flow

### Request Path:
```
Activity 
  → ViewModel (optional)
    → Repository (optional)
      → RetrofitClient.apiService
        → ApiService interface
          → HTTP Request
            → Laravel Route
              → Controller Method
                → Model
                  → Database Query
                    → Database
```

### Response Path:
```
Database
  → Model (Eloquent)
    → Controller (format response)
      → JSON Response
        → HTTP Response
          → Retrofit Callback
            → Coroutine Handler
              → Activity/Fragment
                → Update UI
```

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────────┐
│         Android App (Client)                 │
│  • HTTPS (production)                        │
│  • Request timeout                           │
│  • Error handling                            │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│         Laravel Middleware                   │
│  • CORS handling                             │
│  • Rate limiting (optional)                  │
│  • Authentication (optional)                 │
│  • Validation                                │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│         Controller Layer                     │
│  • Input validation                          │
│  • Business logic                            │
│  • Error handling                            │
│  • Response formatting                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│         Database Layer                       │
│  • SQL injection protection                  │
│  • Query optimization                        │
│  • Transaction handling                      │
└─────────────────────────────────────────────┘
```

---

## 📊 API Response Format

```json
{
  "success": true,      // ← Boolean (berhasil/gagal)
  "message": "...",     // ← String (informasi)
  "data": [...],        // ← Array/Object (data actual)
  "error": "..."        // ← String (hanya jika error)
}
```

### Example Success Response:
```json
{
  "success": true,
  "message": "Data buku berhasil diambil",
  "data": [
    {
      "id": 1,
      "judul": "Laskar Pelangi",
      "penulis": "Andrea Hirata",
      "tipe": "tahunan",
      "tahun_terbit": 2005,
      "description": "...",
      "foto": "buku/laskar.jpg",
      "foto_url": "http://localhost:8000/storage/buku/laskar.jpg"
    }
  ]
}
```

### Example Error Response:
```json
{
  "success": false,
  "message": "Buku tidak ditemukan",
  "data": null
}
```

---

## 🎯 Testing Checklist

### ✅ Laravel API Testing:
```bash
# 1. Test server running
curl http://localhost:8000

# 2. Test API endpoint
curl http://localhost:8000/api/buku

# 3. Test specific buku
curl http://localhost:8000/api/buku/1

# 4. Test search
curl "http://localhost:8000/api/buku/search?q=laskar"
```

### ✅ Android App Testing:
1. Build successful ✓
2. App launches ✓
3. Splash screen appears ✓
4. Navigate to Dashboard ✓
5. Load buku from API ✓
6. Search works ✓
7. Click buku shows detail ✓
8. Images load correctly ✓

---

## 🔧 Debug Tools

### Laravel:
- **Logs**: `storage/logs/laravel.log`
- **Debug Bar**: Laravel Debugbar (optional)
- **Telescope**: Laravel Telescope (optional)
- **Postman**: API testing

### Android:
- **Logcat**: View logs di Android Studio
- **Network Inspector**: Lihat HTTP requests
- **Layout Inspector**: Debug UI
- **Retrofit Logging**: Sudah enabled

---

## 💡 Pro Tips

### Laravel:
```php
// Enable query logging
DB::enableQueryLog();
// Your query
dd(DB::getQueryLog());
```

### Android:
```kotlin
// Add tag untuk Logcat
private val TAG = "DashboardActivity"
Log.d(TAG, "Response: $response")
```

---

**Diagram ini menunjukkan bagaimana Android app terhubung dengan Laravel API! 🚀**
