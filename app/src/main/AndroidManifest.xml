<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.Perpustakaan"
        tools:targetApi="31">
        
        <!-- Splash Activity as Launcher -->
        <activity
            android:name=".activity.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Perpustakaan.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <!-- Dashboard Activity -->
        <activity
            android:name=".activity.DashboardActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar" />
        
        <!-- Detail Buku Activity -->
        <activity
            android:name=".activity.DetailBukuActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar" />
        
        <!-- Buku Kategori Activity -->
        <activity
            android:name=".activity.BukuKategoriActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar"
            android:parentActivityName=".activity.DashboardActivity" />
        
        <!-- Cart Activity -->
        <activity
            android:name=".activity.CartActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar"
            android:parentActivityName=".activity.DashboardActivity" />
        
        <!-- Student Selection Activity -->
        <activity
            android:name=".activity.StudentSelectionActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar"
            android:parentActivityName=".activity.CartActivity" />
        
        <!-- QR Scanner Activity -->
        <activity
            android:name=".activity.QRScannerActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar"
            android:screenOrientation="portrait"
            android:parentActivityName=".activity.StudentSelectionActivity" />
        
        <!-- Manual Student Input Activity -->
        <activity
            android:name=".activity.ManualStudentInputActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar"
            android:parentActivityName=".activity.StudentSelectionActivity" />
        
        <!-- Checkout Activity -->
        <activity
            android:name=".activity.CheckoutActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar" />
        
        <!-- Idle Splash Activity (muncul saat tidak ada aktivitas 3 menit) -->
        <activity
            android:name=".activity.IdleSplashActivity"
            android:exported="false"
            android:theme="@style/Theme.Perpustakaan.NoActionBar"
            android:launchMode="singleTop" />
        
        <!-- Main Activity (Optional) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.Perpustakaan" />
            
    </application>

</manifest>